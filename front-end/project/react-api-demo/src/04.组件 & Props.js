import React from 'react';

export default () => (
  <div>
    <pre>
      1.react 组件分类
        函数组件与类组件
    </pre>
    <pre>
      2.react 组件命名规则
        组件名称必须以大写字母开头，小写字母开头的组件视为原生 DOM 标签
    </pre>
    <pre>
      3.react 组件规则
        所有 React 组件都必须像纯函数一样保护它们的 props 不被更改
    </pre>
  </div>
);
