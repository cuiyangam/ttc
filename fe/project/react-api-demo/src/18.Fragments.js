import React from 'react';

export default () => (
  <div>
    <div>
      React.Fragment允许你将子列表分组，而无需向 DOM 添加额外节点,
      目前，key 是唯一可以传递给 Fragment 的属性
      简写：<></>, 不支持key属性
    </div><br/>
  </div>
);
